node_modules/
# 每项目密钥：scenarios/<project>/.env 不入库，仅保留 .env.example 模板
**/.env
!**/.env.example
.state/
*.log
dist/
.claude/settings.local.json
# client 项目（如天印）为独立 GitLab clone，挂在此路径，不入公共引擎库
/scenarios/tianyin/
